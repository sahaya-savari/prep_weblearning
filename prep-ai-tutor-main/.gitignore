# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── ENVIRONMENT FILES — NEVER COMMIT THESE ──────────────
# Even though frontend .env only has VITE_API_URL (safe),
# blocking all .env files prevents ANY accidental key leak.
.env
.env.local
.env.development
.env.production
.env.staging
.env*.local
